version: '3.8'

services:
  app:
    build:
      context: "/home/schweppa/work/postgres/postgres"
      dockerfile: Dockerfile
    container_name: myapp_go
    ports:
      - "9055:9055"
    environment:
      - bd=postgres://postgres:123@10.160.14.77:5433/mydb
    networks:
      - app_network
    restart: unless-stopped

networks:
  app_network:
    driver: bridge